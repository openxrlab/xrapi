<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.standardar.service">

    <uses-sdk tools:overrideLibrary="com.standardar.sensor"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:multiArch="true"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="@string/Binder"
            android:value="@string/TRUE" />
        <meta-data
            android:name="@string/MultiAlgorithm"
            android:value="@string/TRUE" />
        <meta-data
            android:name="@string/RGBD_SLAM"
            android:value="${RGBD_SLAM}" />
        <meta-data
            android:name="@string/HAND_GESTURE"
            android:value="${HAND_GESTURE}" />
        <meta-data
            android:name="@string/CLOUD_ANCHOR"
            android:value="${CLOUD_ANCHOR}" />
        <meta-data
            android:name="@string/MARKER"
            android:value="${MARKER}" />
        <meta-data
            android:name="@string/FACE_MESH"
            android:value="${FACE_MESH}" />
    </application>

</manifest>